Lobby track: ComputerBits_mastered.mp3 (same filename as lobbyMusic.ts).

Upload to Lens Grove (IPFS-backed, immutable on Base mainnet):
  npm run grove:upload-lobby

Then set NEXT_PUBLIC_LOBBY_MUSIC_URL to the printed gateway_url.
Docs: https://lens.xyz/docs/storage/usage/upload
